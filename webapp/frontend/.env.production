# Production build (npm run build) loads this on top of .env and overrides it.
# Empty base URL → the app uses RELATIVE API paths and talks to whatever origin
# served it (FastAPI on :8000). `.env` keeps the localhost:8000 value for `npm run
# dev`, which runs the frontend and backend as two separate origins.
VITE_API_BASE_URL=
